<Window x:Class="TandA.Views.GroupSupervisorsView"
       xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TandA.Views"
        xmlns:syncfusion="http://schemas.syncfusion.com/wpf" 
        mc:Ignorable="d"
        xmlns:Notification="clr-namespace:Syncfusion.Windows.Controls.Notification;assembly=Syncfusion.SfBusyIndicator.WPF"
        Title="{Binding WindowTitle}" Height="400" Width="570">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />

        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0">
            <StackPanel>
                <Border Background="CornflowerBlue">
                    <StackPanel>
                        <TextBlock Text="List Group Supervisors" Foreground="White" FontSize="30" Margin="5 10 0 0" />
                        <TextBlock Text="List of all Supervisors in a specified Group" Foreground="White" FontSize="15" Margin="5 0 0 10" />
                    </StackPanel>
                </Border>
            </StackPanel>
            <StackPanel Orientation="Horizontal" Margin="10 5">
                <TextBlock Text="Group:" Width="50" />
                <ComboBox DisplayMemberPath="GroupRef" Width="150" ItemsSource="{Binding Groups}" SelectedItem="{Binding Group}"></ComboBox>
                <Button Background="Green" Width="100" Foreground="White" Content="Add Supervisor" Click="Add_Supervisor_Click" Margin="10 0 0 0" />
                <Button Background="Green" Width="120" Foreground="White" Content="Delete Supervisor(s)" IsEnabled="False" Margin="10 0 0 0" />
            </StackPanel>
        </StackPanel>
        <!-- https://www.youtube.com/watch?v=Ckz04z2NtTU -->
        <DataGrid x:Name="dataGrid" SelectionMode="Single" Grid.Row="1" AutoGenerateColumns="False" SelectionUnit="FullRow"  ItemsSource="{Binding Supervisors}" SelectedItem="{Binding Employee}" >

            <DataGrid.CellStyle>
                <Style TargetType="{x:Type DataGridCell}">
                    <!--<Setter Property="Background" Value="Transparent" />-->
                </Style>
            </DataGrid.CellStyle>

            <DataGrid.Columns>
                <DataGridTemplateColumn CanUserSort="True">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <CheckBox />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Width="*" Binding="{Binding EmployeeNumber}" Header="REFERENCE"/>
                <DataGridTextColumn Width="*" Binding="{Binding Firstname}" Header="FIRSTNAME"/>
                <DataGridTextColumn Width="2*" Binding="{Binding Lastname}" Header="LASTNAME"/>
            </DataGrid.Columns>
        </DataGrid>


    </Grid>
</Window>
