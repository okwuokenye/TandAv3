<Window x:Class="TandA.Views.SelectPayPeriodView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TandA.Views"
        mc:Ignorable="d"
        Title="Select Pay Period" Height="150" Width="300">
    <Grid>
        <StackPanel Margin="5 20">
            <StackPanel>
                <TextBlock Text="Select Pay period Start" />
                <ComboBox ItemsSource="{Binding Periods}" SelectedItem="{Binding Period}" DisplayMemberPath="sPeriodStart" />
            </StackPanel>
            <Button Content="Preview HR Report" Width="150" Margin="0 10" Click="Button_Click"/>
        </StackPanel>
    </Grid>
</Window>
